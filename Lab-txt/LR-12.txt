Лабораторная работ 12. Определение нестандартных запросов
Постановка. Необходимо написать запрос к БД и определить к нему АПИ «Получить
название категории товара с заданным идентификатором»
1. Некоторые изменения настроек БД
1.1 Генерация названий полей в snake_case вместо camelCase
Внесем правки в определение параметров инициализации БД app/models/index.js
Добавим блок define
Это необходимо, что имена полей в модели транслировались в snake_case (через знак
подчеркивания, underscore) в скрипты создания таблиц БД. Это в свою очередь
необходимо для соблюдения конвенции наименования полей таблиц в PostgreSQL и
последующей корректной работе запросов.
NOTE! Не забудьте указать флаг принудительного пересоздания схемы БД. А потом его
убрать в файле app/server.js
После пересоздания БД имена полей таблиц изменяться.
2. Дополнить контроллер
В файл контроллера app/controllers/goods.controller.js добавить метод
getGoodsGroupName
NOTE-1!
Обратите внимание, что при вызове метода запроса query() мы определяем SQL запрос.
Также указываем в качестве параметров query(), тип возвращаемого значения type:
QueryTypes.SELECT -–это указывает на то, что будет возвращен результат выполнения
запроса без метаданных.
NOTE-2!
Метод query возвращает массив объектов. В данном случае, нам нужно взять первый
элемент с индексом 0.
3. Добавляем метод в маршрутизатор app/routes/goods.routes.js
NOTE! Обратите внимание, что в маршруте к методу используется параметр –
идентификатор товара
4. Протестируем запрос
4.1. При необходимости через АПИ создайте тестовые данные по товара и товарным
группам
4.2. В Postman выполнить запрос http://localhost:8080/api/goods/3/goodsgroupname
5. Изменим запрос так, чтобы возвращался вся запись о товарной группе.
Добавим новы метод GET api/goods/:id/goodsgroup
Модифицируем определение метода query()
NOTE-1! Обратите внимание, что как результат мы возвращаем все атрибуты таблицы
goodsgroup gg, как gg.*. Это указывает Sequelize на то, что необходимо получить весь
объект.
NOTE-2! Мы использовали дополнительные настройки model: GoodsGroup, mapToModel:
true, что ответ был представлен в виде объекта модели, который мы определили в
Sequelize.
6. Параметризация запроса
6.1. Использование строки-подстановки.
В п2. Мы уже использовали параметризацию через сроку-подстановку.
Для этого строковое выражение необходимо взять в обратные кавычки `SELECT 1` и
использовать встраиваемые подстановки через использование конструкции ${}. В
фигурных скобках нужно указать имя переменной или объекта из контекста выполнения
метода.
Например, в п 2. Мы подставляли значение переменной id
`SELECT gg.* FROM goodsgroups gg LEFT JOIN goods g ON gg.id = g.goods_group WHERE g.id
= ${id}`
6.2. Использование параметров замещения.
Sequelize позволяет использовать более формальный подход с параметрами запроса.
Воспользуемся официальной документацией Sequelize
(https://sequelize.org/docs/v7/querying/raw-queries/)
// This query uses positional replacements
await sequelize.query('SELECT * FROM projects WHERE status = ?', {
 replacements: ['active'],
});
// This query uses named replacements
await sequelize.query('SELECT * FROM projects WHERE status =
:status', {
 replacements: { status: 'active' },
});
Модифицируем метод query() в АПИ api/goods/:id/goodsgroup
NOTE! Само выражение запроса SELECT мы поместили в обычные (прямые) одинарные
кавычки.
Проверим результат в Postman
7. Задания для самостоятельного выполнения.
7.1. Определить 5-7 методов с использованием пользовательских запросов в БД (raw
query) с параметрами и маппингом на объекты сущностей приложения.